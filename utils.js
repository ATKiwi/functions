function product(a, b) {
    return (a, b)  
}

function divide(x, y) {
    let z = x / y
    return z 
}

function power(i, o) {
    let c = Math.pow(i,o)
    return c 
}
function subtract(a, b) {
    let c = a - b
    return c
}